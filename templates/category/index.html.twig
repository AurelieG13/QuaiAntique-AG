{% extends 'base.html.twig' %}

{% block title %}Hello CategoryController!{% endblock %}

{% block body %}
    <h1>Categories</h1>

    <table class="table">
        <tbody>
        {% for category in categories %}
            <tr>
                <td>{{category.name}}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
